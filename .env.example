HOST="http://localhost:8080"
NAME=asa_agent
TOKEN=

# PDDL solver (planning-as-a-service). Leave default if running via `npm run planner:up`.
PAAS_HOST=http://localhost:5001
# PAAS_PATH defaults to /package/dual-bfws-ffparser/solve — override only if you switch planner.

